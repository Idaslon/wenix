mutation Login($data: LoginInput!) {
  login(data: $data) {
    token
    user {
      id
      name
      email
    }
  }
}

mutation Register($data: RegisterInput!) {
  register(data: $data) {
    id
    name
    email
  }
}
